[server]
headless = true
port = 8501
enableCORS = false

[browser]
gatherUsageStats = false

[theme]
# EXL brand colours
primaryColor             = "#C9A84C"   # EXL gold — buttons, active tabs, highlights
backgroundColor          = "#18140F"   # Deep warm black (EXL dark canvas)
secondaryBackgroundColor = "#2C2117"   # Elevated panels / sidebar
textColor                = "#F5EDD6"   # Warm off-white
font                     = "sans serif"
